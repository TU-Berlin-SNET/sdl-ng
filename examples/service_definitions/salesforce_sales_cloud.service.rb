name "Salesfoce Sales Cloud"

has_rest_interface
has_soap_interface
has_xmlrpc_interface

has_browser_interface {
  compatible_browser "abc" do
    min_version "10.0"
  end

  compatible_browser "abc" do
    min_version "10.0"
  end

  compatible_browser "abc" do
    min_version "10.0"
  end
}